<h1>Select Spam Type</h1>


<%= form_with(url: '/new/2') do |form| %>

  <div class="field">
    <%= form.label :spam_type %>
    <%= form.collection_select :spam_type_id, SpamType.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit 'Select' %>
  </div>
<% end %>
